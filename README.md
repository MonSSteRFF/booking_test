# init project

- ```pnpm install``` - install deps
- ```pnpm run generate:types``` - generate open-api schema in back and types for frontend
- ```pnpm run dev``` - run docker-compose with back+front app
